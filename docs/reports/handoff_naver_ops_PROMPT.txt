============================================================
 네이버 블로그 세션에 붙여넣을 프롬프트
------------------------------------------------------------
 작성  : 2026-08-07 (혼살림 세션 #52)
 쓸 때 : 2026-08-24(월) 캘린더 알림 — 단, 아래 [지금 바로]는 즉시 사용 가능
 어디서: D:\naver_blog 세션 (혼살림 세션에서 하지 말 것)
 상세  : D:\affiliate_hub\docs\reports\handoff_naver_ops.md
============================================================


■ 1단계 — 새 세션을 열고 먼저 이것부터

/naver-start


■ 2단계 — 브리핑이 끝나면 아래 블록을 그대로 복사해 붙여넣기
------------------------------------------------------------
혼살림(D:\affiliate_hub)에서 무인 운영 관리층(ops 세션)을 설계·검증했고,
그걸 이 프로젝트에 이식하려고 한다.

인수인계 문서가 준비돼 있으니 먼저 읽어라:
D:\affiliate_hub\docs\reports\handoff_naver_ops.md

그 문서가 지정한 참고 파일들(honsalim-ops.md, DECISIONS KK절, ops 리포트,
EVENTS #52)도 함께 읽고, naver_blog에 맞게 어떻게 이식할지 계획을 제안해라.
★D:\affiliate_hub는 읽기만 하고 절대 수정하지 마라(프로젝트 분리).

그리고 별건으로 먼저 확인할 것: 문서 §7에 LLM 공급자 장애 재확인 근거가 있다.
혼살림이 같은 계정·같은 모델로 08-06·07에 긴 본문 생성에 성공했으니
회복 여부를 확인해라(확인은 공짜).
------------------------------------------------------------


■ [지금 바로] LLM 회복 확인만 급히 하려면 — 아래만 붙여넣기
------------------------------------------------------------
LLM 공급자 장애(deepseek-v4-pro 긴 생성 불가)로 재고 보충이 막혀 있는데,
혼살림(D:\affiliate_hub)에서 다음이 실측 확인됐다:

- 혼살림이 08-06·08-07에 같은 모델로 본문 3,283자·3,514자 생성 성공
  (출력 토큰 8,883·9,483 — 실패했다던 길이대) [확정, logs/auto_cycle.log]
- 두 프로젝트가 같은 OpenRouter 계정을 쓴다 [확정]
  naver_blog 문서 "크레딧 25 − 사용 18.46" = 혼살림 조회 "총 25 · 사용 18.64"
- naver_blog가 겪은 건 429 temporarily rate limited = 일시적 속도 제한

따라서 지금은 회복됐을 가능성이 높다. 회복 여부를 확인하고(확인은 공짜),
회복됐으면 재고 보충 절차를 안내해라. 8/10부터 발행 정지 예정이라 시급하다.

덤: TODO #3의 "계정 공유 의심 [추정]"은 확정이다 — 혼살림이 매일 1편씩
생성하며 월 $0.4가량 쓴다(전체 월 $0.86 중). 잔액 $6.36 → 2~3개월 뒤 충전 필요.
------------------------------------------------------------


■ 왜 긴 내용을 붙여넣지 않아도 되나

오늘 작업은 전부 파일로 남아 있다. 대화는 사라져도 파일은 남는다.
인수인계 문서(handoff_naver_ops.md)에 그 세션이 필요한 것을 다 넣었다:

  - 읽을 파일 5개와 순서
  - ops 4단계 뼈대 (점검 → 분석 → 허용범위 실행 → 보고)
  - 반드시 옮겨야 할 안전 원칙 (실패 시 무변경 · 테스트 게이트 ·
    허용범위 표 · 새 예약작업 금지 · 파일럿 먼저)
  - naver_blog가 이미 가진 것 (중복 구축 방지)
  - 혼살림과 다른 점 (GSC 없음 · 배너가 글의 재료 · 세션 30일 만료)
  - 두 채널이 독립적으로 도달한 같은 결론 (롱테일 법칙)
  - 선행 조건 (혼살림 ops 2~3회 검증 — 미완이면 이식 금지)
  - 금지 사항 (프로젝트 분리 · 코드 그대로 복사 금지)


■ 순서 (잊지 말 것)

  8/13(수)  혼살림에서 /honsalim-ops 2회차 단독 실행 → 소모량 실측
  8/20경    3회차 → 패턴 확정
  8/24(월)  이 프롬프트로 naver_blog 이식        ← 캘린더 알림 등록됨

  ※ 8/13 검증이 안 끝났으면 이식을 미룰 것.
     검증 안 된 패턴을 복제하면 결함까지 복제된다(#48 교훈).
