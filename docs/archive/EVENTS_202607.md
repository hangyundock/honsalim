# EVENTS_202607.md — 혼살림 세션 로그 아카이브 (2026년 7월)

> EVENTS.md 자동 회전으로 이동된 옛 세션 원문. 인덱스는 EVENTS.md "ARCHIVE 인덱스" 참조.

---

### 세션 #41 — 2026-07-03 (Opus 4.8 / Fable 5, ★무인 발행 자가복원 근본수정(반려 조용한 데드엔드 해소) + 반려 재시도 원클릭 + 3줄 안내 배너 + 텔레그램 푸시(TIMA 봇 재사용), 회귀 996→1037, main 배포·라이브 검증)

**시작 상황**: `/honsalim-start`(#40 a8697b8). 주인이 대시보드 사진과 함께 "미리 세팅한 쿠팡 키워드가 무인 발행 제대로 안 된 것 같다 — 쿠팡 html 저장한 게 모니터받침대·무중력·등받이·메쉬·학생용·허리편한의자 아닌가?" 지적. 추측 금지·운영 DB(`data/honsalim.db`) 직접 조사 + 6하위시스템 병렬 코드조사(워크플로우).

**핵심 [확정]** (검증·main·라이브. 상세 DECISIONS Z):
1. **★진단 — 조용한 데드엔드 실측**: 쿠팡 배너 저장 7키워드(6+게이밍의자) 중 3만 라이브(모니터받침대·무중력·학생용)·**3개 게이트 반려 방치**(등받이·허리편한=`headings_keyword_low` 소제목 키워드 0 / 메쉬=truth `1인칭 "2년 사용"`)·1개 옛 미발행. 시스템은 방치분 대신 매일 새 키워드 추가하며 넘어감(쿠팡 준비분 썩음).
2. **★Z1 자가복원**: 반려 키워드가 `drafted`로 굳어 `auto_pick`(pending만)이 영영 재선정 안 하던 §0 위반 → `fail_count`++·상한3 미만 자동 `pending` 복귀·초과 `failed` 격리 + digest 3집계 + [ALERT]. **라이브 실증**: 07-03 사이클이 '서재책상' 자동 반려→fail_count=1 재시도 복귀(첫 작동).
3. **★Z2 재생성 실행지시화**: 반려 raw 되먹임 → 실행지시 변환(headings_keyword_low → "소제목을 '<키워드>'로 시작"). 게이트 정상(`_ns` 공백무시)=생성 지시가 원인·게이트 약화 0.
4. **★Z3 반려 재시도 원클릭**: CLI `keyword-requeue` + 대시보드 [🔁] 버튼 + `update_and_requeue.bat`. 안전가드=발행 글 연결 차단·최신 draft 반려만. 주인 실행→막힌 4개 pending 복귀 확인.
5. **★Z4 배너+텔레그램**: 3줄 배너(상태/쿠팡 재고 런웨이/할 일·심각도색)+`common/notify.py`(일일 리포트=하트비트·경보=쿠팡 소진/반려/발행0). **TIMA `settings.json` 봇 재사용**→`telegram.env`→**실발송 성공**.
회귀 996→**1037**(+41). 클린. main 3커밋(0fa8b25·cb12c6a·1a8ed39)·운영 ff 동기화.

**무인·안전(§0)**: fail-safe 유지 + 새 fail-loud(반려 자가복원·텔레그램 하트비트). 텔레그램 미설정/실패는 사이클 무영향. PowerShell bypass .bat 권한 차단→순수 bat 재작성.

**잔존/다음(#42)**: ①**무인 재생성 관찰**: 복귀한 4개+서재책상이 개선된 재생성(실행지시)으로 실제 발행되는지 매일 확인(하루 1편·나흘 순차)·재반려 시 자동재시도·상한 도달 [ALERT]/텔레그램. ②**★성장 최우선**([[growth-first-priority]]): 색인 커버리지(#40 4→증가) 관찰·핵심 URL 색인 요청. ③(이월) 씨앗 커버리지·E-E-A-T(Person Schema)·미발행 2편(게이밍의자·노트북받침대) 검토 후 결정.
