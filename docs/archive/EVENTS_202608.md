# EVENTS_202608.md — 혼살림 세션 로그 아카이브 (2026년 8월)

> EVENTS.md 자동 회전으로 이동된 옛 세션 원문. 인덱스는 EVENTS.md "ARCHIVE 인덱스" 참조.

## 이 파일의 세션 인덱스

- 세션 #48 (2026-07-31~08-01 무인 발행 3일 중단 근본해결(밀도 벽 돌파·B2 절대 하한)·라이브 발행 2편·비용 낭비 방지 5종(cli experiment·토큰 추적)·slug 근본수정·회귀 1126→1230)

---

### 세션 #48 — 2026-07-31~08-01 (Fable 5, ★무인 발행 3일 중단 근본해결(밀도 벽 돌파·B2 절대 하한) + 라이브 발행 2편 + 비용 낭비 방지 5종 + slug 근본수정, 회귀 1126→1230, main 10커밋)

**시작 상황**: `/honsalim-start`(#47). 브리핑 중 **07-30·07-31 발행 0편**을 실측 적발 — 침묵 정지가 아니라 `책상추천`이 seo 게이트 `density_high`로 2연속 반려. 주인이 원인 규명 → 수정 → 라이브 테스트를 순차 지시. 후반 주인이 **"결과를 한 번에 내지 말고 비판적 정밀 재검수 후 최종결과"** 원칙을 **모든 결과물로 상시 확대** 지시.

**핵심 [확정]** (상세 **DECISIONS FF1~FF8**):
1. **밀도 벽의 정체(FF1·FF2)**: LLM이 횟수를 센 게 아니라 **문체를 바꿨다**(소제목·상품 문단·FAQ마다 기계 배치 = 31회). 그리고 모델은 **"탈락"이라 쓴 조건만 추적**한다 — 옛 지시는 하한에만, 내 1차 수정은 상한에만 경고를 달아 **양쪽으로 번갈아 탈락**(31회 도배 ↔ 7회·소제목 0개 미달). 세 경계를 같은 무게로 명시해 해결.
2. **★B2 절대 횟수 하한(FF3·주인 결정)**: 라이브 6개 본문 실측 — '~추천' 검색어 복합어는 도배 금지 시 자연 사용량이 **4~7회로 고정**돼 %하한은 긴 글에서 반드시 뚫린다(결정 A(0.8%)도 5,199자 글 0.41%로 즉시 뚫림). → 키워드 글 하한을 `min_count>=4`로 전환. 상한·도배 금지·위치 게이트 불변.
3. **1인칭 오탐 반전(FF4)**: "2~3년 사용에도 견고하다는 후기"·"1~2년 사용 후 이동"이 반려돼 재시도를 소모(3회 중 2회). 1차 블랙리스트가 곧 재발 → **경험 동사 화이트리스트**(했·해보·썼·중이). 재시도 2→3(주인 승인).
4. **★라이브 발행 2편**: `kw-269a4bdb`(책상추천·01:21) + `1-2`(1인용밥솥·08-01 11:11 **정규 사이클 시도 1회 통과·개입 0**). 라이브 200 검증. 글 **18→20편**.
5. **비용 낭비 방지 5종(FF6)**: 라이브 6회·15콜·3시간의 원인은 ①n=1 라이브 판단 루프 ②LLM 사용량 미기록(원화 환산 불가). → CLAUDE.md §7-3 대응 순서 + `cli experiment`(오프라인 N샘플 분포·기본 dry-run) + migration 010 토큰 추적(재시도 전량) + 본문 예산 정합화 + 캐시 관찰성.
6. **★캐시 조사 종결(FF7)**: 실측 input 27,687 중 **16,448(59%) 적중** — 제공자 자동 캐시 작동, 배선 복구 **불요**.
7. **slug 근본수정(FF8)**: `slugify`가 한글을 버리고 **남은 조각**을 slug로 씀(`1인용밥솥`→`1`→`1-2`). 부분 유실도 해시 폴백 + **큐에 굳은 조각 자가교정**(`32인치모니터암`이 `32`로 대기 = 다음 발행에 `/articles/32/` 될 뻔). 라이브 3건 URL은 주인 결정으로 유지.

**자체 재검수로 잡은 결함** — 주인 지시 원칙의 실효 증거: 목표 밀도 1.2→1.4 정정(실패 방향 비대칭) · 부동소수점 드리프트 · 카테고리 경로 누락 · 길이가 약분되는 무늬만 테스트 · **배선 검증 공백**(단위테스트만으론 백스톱을 안 불러도 초록불) · **실험 하네스가 운영과 다른 프롬프트 생성**(season_peak 누락 — 측정 도구가 틀리면 결론도 틀린다) · 하네스의 가짜 미달 표시 · slugify 수정만으론 큐의 굳은 slug 미해결 · 비용 보고 "47% 회피 가능"이 과장(실제 20%).

**무인·안전(§0)**: 자가복원 4겹째 = `enricher/density_fix.py` — density_high만 남으면 LLM 재호출 없이 초과분을 대체어로 감산(제목·고지·소제목·첫 등장 불가침, 조사 안전 판정, 5게이트 재검증 후 실패 시 원문 복귀). 실측 재생 4.33%→1.42%·3.97%→1.43% 둘 다 통과.

**잔존/다음(#49)**: ①**[관찰] 본문 예산 부작용** — 산문 25~30%↓로 출력 토큰은 줄었으나 키워드 자연 출현이 4~7→**3~5회**로 내려가 min_count=4 여유 축소(3샘플 중 1건 count_low). 며칠 발행 안정성 관찰 ②[입력] LLM 단가(`llm_price_*_per_1m`) — 넣기 전까지 비용 $0 표시 ③[이월] GSC 색인 전환·도마 클러스터 편중·여름이불 자동비공개·draft 카테고리 3개·IndexNow 관찰성·쿠팡 부트스트랩 ④(선택) 추적 미연결 경로(카테고리 가이드·비전 게이트).
