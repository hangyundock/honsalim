# EVENTS.md — 혼살림 세션 로그

> 자동 회전: 6번째 세션 시 가장 옛 세션이 docs/archive/EVENTS_YYYYMM.md로 이동.
> 옛 세션 검색: 아래 월 인덱스로 파일 특정 → **그 파일 머리의 세션 인덱스** 확인 → 본문 grep.
> Cap: 20KB. ※#48에서 세션별 한 줄 요약을 각 월 파일로 이관 — 인덱스가 세션 수에 비례해
> 무한 증가하며 cap을 잠식하던 구조 문제 해결(23.7→16.0KB).

## ARCHIVE 인덱스 (월 단위 — 세션별 한 줄 요약은 각 파일 머리에 있음)

- [EVENTS_202608.md](archive/EVENTS_202608.md) — 세션 #48 (1건)
- [EVENTS_202607.md](archive/EVENTS_202607.md) — 세션 #41~#47 (7건)
- [EVENTS_202606.md](archive/EVENTS_202606.md) — 세션 #19~#40 (22건)
- [EVENTS_202605.md](archive/EVENTS_202605.md) — 세션 #1~#18 (16건)

## 최근 5세션

### 세션 #52 — 2026-08-07 (Fable 5, ★GSC API 연동+색인 커버리지 자동측정 + 무인 운영 세션 `/honsalim-ops` 신설·파일럿 + GSC 쿼리 주간 적립 + 추천창 재설계(포화 판정 자동화) + 씨앗 8종, 회귀 1329→1369, main 9커밋)

**시작 상황** — `/honsalim-start`(#51). 브리핑에서 최우선 관찰(08-06 1일차)이 **이미 이틀치 성공**임을 실측. 주인 "순위·트래픽 심층 분석" → 성장 진단 → 씨앗 등재 → **"매번 세션 여는 게 소모적이다, 주기적 자동 관리가 가능한가"** → GSC 연동·ops 세션 구축으로 확장.

**실행 결과 [확정]** (상세 DECISIONS JJ·KK):
1. **무인 1·2일차 전항목 통과** — 공략구간 픽(1,130·890)·배포 게이트 차단 0·균형 회전·score≠0. 발행 24편·라이브 200.
2. **★GSC API 연동(KK1)** — `tracker/gsc.py`+`gsc-report`. 서비스 계정(무인·무료). 조직 정책이 키 발급을 막아 **개인 계정 프로젝트로 우회**, 서비스 계정을 속성에 초대(계정 달라도 무관). 순위·노출을 스크린샷 없이 코드로 읽게 됨.
3. **★색인 커버리지 자동측정(KK2)** — sitemap API의 `indexed`가 **구글 폐기 필드(항상 0)**임을 자체 적발(실제 27인데 0). 그대로 뒀으면 무인 보고가 매주 거짓 경보. → URL 검사 API로 URL별 판정. 실측 **27/42(64%)**·미색인 15(Discovered 9·Unknown 5·Crawled-not-indexed 1). 주인 색인 요청 11건 실행('unknown' 5건 전부 커버).
4. **★`/honsalim-ops` 무인 운영 세션(KK3)·파일럿 1회** — 점검→분석→**허용범위만 실행**(JJ 원칙+테스트 게이트)→보고(파일·텔레그램·커밋). 텔레그램 **도착 확인**(주인 스크린샷). 실행 0건(의도적 절제).
5. **★네이버↔구글 무상관 → 자체 기각(JJ4·KK4)** — 1차 해석("네이버 신호가 틀렸다")을 같은 세션에서 **반증·기각**: 노출 66 중 **54(82%)가 익명화 쿼리** = 목표 키워드가 아닌 초롱테일에서만 노출. '8.3위'는 **생존편향**. → 판정 불가·현행 유지, 재평가 기준을 수치로 고정(월 노출 500+ 또는 비익명 쿼리 30+).
6. **★GSC 쿼리 주간 적립(KK5)** — migration 011 + `gsc_store`. 새 예약 작업을 만들지 않고 **기존 월요일 훅에 얹음**(#51 II10). 창 겹침 이중계산을 피해 합이 아닌 **최대값** 집계. 라이브 적립 9쿼리.
7. **★추천창 재설계(KK6, 주인 지적)** — "포화인지 사용자가 구분해야 하면 추가를 못 한다". `cluster_balance` 신설: 정렬을 **발행가능→얇은 묶음→점수**로(맨 위가 항상 최선), 발행불가·포화 기본 숨김+토글, 묶음 칸(N편·⚠많음), 요약 한 줄. limit 30→80(숨김 시 1건만 남던 실측·비용 동일). ⭐1순위 버튼 함정 해소.
8. **씨앗 8종 등재(JJ1~JJ3)** — 시즌 가설 절반 기각(신학기·재택 20~280 미달). monitor-stand 4종=**HH8 종결**·원룸책상·수험생의자·선제 2종.
9. **§12 정정(KK7, 주인 지시)** — "환경 변경 금지"의 근거가 **키움 HTS의 32-bit 요구**임을 명시. Python은 유지, 그 외는 기존 작동 무영향 검증 시 향상 허용. 규칙은 이유를 알고 적용.
10. **비용 확정** — OpenRouter 잔액 **$6.36/25** · 혼살림 월 **$0.4 미만**(실측 토큰×공시단가). GSC·네이버·CF·Actions·적립 전부 0원. **새 결제 불요**, 2~3개월 뒤 충전.
11. **교차 발견** — naver_blog(네이버)도 "대표 키워드 유입 0·롱테일만"으로 **같은 결론**. 채널이 달라도 같으면 신생 매체의 구조적 법칙 [추정]. 또 naver_blog의 'LLM 장애·계정 공유 의심'에 혼살림 실측이 답을 줌(같은 계정 [확정]·같은 모델 긴 생성 08-06·07 정상).

**자체 적발·정정 4건** — [[critical-final-review]] 실효: ①sitemap indexed 폐기 필드 오독 ②네이버-구글 해석 기각(생존편향) ③씨앗 커버 판정 정확일치→포함관계(라이브 9/9 오검출) ④추천 limit 30에서 표시 1건. ★가드 역검증 3회(배선·정렬을 되돌려 실제 실패 확인).

**잔존 미해결** — ops 소모량 분리 측정 실패(다음 회차 단독 실행) · `notify-alert`가 발송 실패에도 rc=0 · 색인 요청 효과 미측정(기준선 27/42) · 도마·의자 과밀(게이밍의자 색인 거부) · 08-02 미기동 · 여름이불 · 레버 C/D 주인 대기.

**다음 세션 할 일** — TODO.md 참조. ★ops 자동화 로드맵 3단계(8/13 소모량 측정 → 8/24 naver_blog 이식 → 텔레그램 통합)는 **구글 캘린더에도 등록**됨.

### 세션 #51 — 2026-08-05 (Opus 5, ★글↔글 내부링크 0 근본수정(0→132) + 키워드 공략구간 하강(2000+→300~2000·후보 94.8% 회복) + og:image 전 42페이지 누락 + ★무인 감시 자동화(배포 전 게이트·주간리포트) + www 301, 회귀 1274→1329, main 5커밋)

**시작 상황** — 주인 질문(GitHub 오픈소스 라이선스)에서 출발해 "왜 성과가 없나"로 옮겨갔다. #50이 무인 리필을 고쳤지만 트래픽은 여전히 없었다(GSC 3개월 노출 73·클릭 2·평균 18.2위).

**실행 결과**

1. **트래픽 진단 [확정, DB·라이브·GSC]** — 첫 발행 06-27 → **사이트 나이 40일**. 클릭률 2/73=**2.7%**로 18위권 평균(1% 미만)보다 높다 → **콘텐츠가 아니라 순위 문제**. 병목 = 도메인 나이·키워드 난이도·검색 외 유입 0.
2. **글↔글 내부링크 0 → 132 [확정, 라이브 전수]** — II1~II3. 렌더 경로 둘 다 배선. 재검수로 쏠림(14→9)·고아(1→0) 추가 수정.
3. **키워드 공략구간 [300, 2000] [확정, 네이버 실측]** — II4. 하한 2000이 후보 1,980개 중 **1,878개(94.8%)를 폐기**. ★1차안(경쟁도 결정자)은 실데이터로 기각(II5). mock 23건 재조정 — 단정문 무약화.
4. **SEO 정밀점검 [확정, 42페이지+HTTP]** — 97건 → 반증 후 **실제 2건**. og:image(II6)가 유일한 중대 결함. 기각: 푸터 H4·noindex sitemap·canonical 통합 중복·description 길이. HTTP 전부 정상.
5. **★무인 감시 자동화 [확정, 주인 승인]** — II7·II8·II10. site_audit 단일 진실원 + refresh_cycle 단일 관문 게이트 + 주간 리포트. doctor §17~§19.
6. **비판적 재검수 [확정, 주인 지적]** — II9. 실제 버그 1(게이트 차단→성공 오판)·미검증 2(주간 침묵·유사도 17분→75ms).
7. **4개 영역 점검 [확정]** — 컴플라이언스 위반 0 / 제휴 317종 일치 / 유사도 0.65+ 6쌍·얇은 글 0 / 클릭 추적 미연결(II12·보류).
8. **www DNS + 301 [확정, 주인 실행]** — II11. 실측 4/4.

**잔존 미해결** — 08-06 무인 사이클 1일차(#50 카테고리 균형 + #51 새 구간·게이트가 라이브에서 처음 도는 날) · 대기 키워드 0(리필 자동보충 확인 필요) · 클릭 추적 미연결(트래픽 후) · 도마 6편 상호경쟁 · 여름이불 결정 · monitor-stand 공급 부족.

**다음 세션 할 일** — TODO.md 참조.

### 세션 #50 — 2026-08-05 (Opus 5, ★무인 리필 '검색량 없이 한 달' 근본원인(load_secrets) 봉인 + 카테고리 균형 + fail-loud + GSC 색인 4→18 + laptop-stand 공개, 회귀 1258→1274, main 2커밋)

**시작 상황**: `/honsalim-start`(#49). 브리핑 중 **#50 최우선 관찰이 이미 성공**임을 실측 확인(08-05 사이클이 `32인치모니터암` 재생성 → seo `skip`→**`pass`** → article 24 발행). 주인 "유입 결과를 알 수 있는가" → 측정 인프라 실측 → **GSC 화면 4장** 제공 → 편중 원인 추적 → 근본수정.

**핵심 [확정]** (상세 **DECISIONS HH1~HH10**):
1. **★★근본 원인 = `cmd_auto_cycle`의 `load_secrets` 누락(HH1)**: 리필이 자격증명 없이 네이버 조회 → 실패 → `recommend`가 예외를 삼켜 **캐시(yml secondary)로 자가강등**. 3중 증거 — 코드 경로 / 리필 투입 10건 **전부 `score=0`** / yml 순서와 발행 순서 완전 일치. **글 생성 경로는 자체적으로 secrets를 불러 LLM이 멀쩡히 돌았기 때문에 한 달간 은폐**([[project_verify_script_load_secrets]]의 운영 코드판).
2. **캐시 강등 = 카테고리 통째 소진(HH2)**: 점수가 전부 동률이라 yml 순서대로 뽑힌다. 리필 시각 `02:11 UTC`로 사람 추가와 구분 — 07-01~03 desk 3연속, **07-20~27 cutting-board 6연속**(GSC 실측 그 6편 상위 노출 0).
3. **균형·경보(HH3·HH4)**: 선정 키 `(직전이면 1, 사용 횟수, -점수)` — 점수는 동률 판정에만·후보 집합 불변(§0). 리필 경로 한정. 캐시 강등은 `degraded`→digest·[ALERT]·텔레그램, 단 발행은 되므로 `abnormal` 제외(#49 과민 경보 교훈).
4. **★GSC 실데이터(HH5)**: #46 색인요청 11개 **전환 확인** — 색인 4→**18**. 3개월 노출 73·클릭 **2**·평균순위 **18.2**. 클릭을 낸 글 = 학생용의자(CTR 16.7%). 미색인 34 중 22는 정상·실제 대기 12. ★병목이 "구글이 모른다"→**"알지만 상위에 안 띄운다"**로 이동.
5. **laptop-stand 공개(HH6·주인 결정)**: draft 3개 모두 재고·가이드가 공개분과 동등함을 실측 → 씨앗 8개를 가진 것만 공개. 라이브 200·sitemap 39→40·기존 글 영향 0. 적격 18→**20**·10일 카테고리 **4종→5종**.
6. **도달 불가 씨앗 5개(HH7)**: core 부분문자열 필터 때문에 무인 추천이 **영원히 못 집는** secondary — monitor-stand는 5개 중 2개(40%)가 죽어 실질 3개(오늘 적격 0개의 이유). `lint_alignment`에 `unreachable` 추가(정규화는 **운영 필터와 같은 함수**). ★매핑 사전 역할은 유효해 **삭제 않고 경고**.

**자체 기각·정정 4건** — [[critical-final-review]]의 실효 증거: ①**1차 설계 기각** — '마지막 사용 시점' 기준은 누적 사용량을 무시해 실데이터에서 도마를 또 뽑았다 → 사용 횟수 기준 전환 ②**씨앗 보강 자체 기각(HH8)** — 후보 3건이 전부 다른 제품(오매핑), 미매핑 20건 전수 검토 결과 적합한 건 전부 과포화 의자이고 `허리디스크의자`(**비YMYL 위반 소지**)까지 섞여 있었다 ③"카테고리 4종 그대로"라 정직 보고 후 카테고리 공개로 5종 달성 ④**가드 실효성 2회 역검증** — 수정을 되돌려 테스트가 실제로 실패함을 확인(무늬만 테스트 방지·#48 교훈).

**무인·안전(§0)**: fail-loud 구멍 1개 봉인(`refill_degraded`) + 근본 원인 1개 제거. 모든 수정에 재발 방지 가드 동반(신규 16건·배선 순서 고정 포함). 회귀 1258→**1274**·클린. main `a6a5340`(푸시·운영 ff 반영) + `9c318f9`. 부수: 주인 GSC 색인요청 2차 실행 · LLM 단가 불요 결정(HH9).

**잔존/다음(#51)**: ①**★[관찰·최우선] 08-06 사이클 1일차** — 도마 아닌 키워드(예상 `1인밥솥`)가 뽑히는가 · 로그에 "리필 검색량 조회 실패"가 **없는가** ②[관찰] laptop-stand 색인 전환 ③★[모니터링] 색인 요청 2차분 ④[미해결] monitor-stand 공급 부족(보강 재료 없음) ⑤(이월) 도마 편중·여름이불·draft 2개·IndexNow 오탐. **상세=TODO.md**

---

### 세션 #49 — 2026-08-04 (Opus 5, ★08-04 발행 0편 복구 + 'validated=5게이트 통과' 구멍 봉인(미검증 글 자동발행 차단) + 씨앗 인치 변형 + 사람 경로 경고 2종, 회귀 1230→1258, main 3커밋)

**시작 상황**: `/honsalim-start`(#48). 브리핑 중 **오늘(08-04) 발행 0편**과 **대기 큐 pending 0**을 실측 적발. 주인이 ①발행 0편 복구 ②큐 보충 경로 검증 ③#48 본문 예산 관찰 순서로 지시. 08-02(일)은 사이클 **로그 자체가 없음**(미기동).

**핵심 [확정]** (상세 **DECISIONS GG1~GG8**):
1. **원인 = 사람이 넣은 미매핑 키워드(GG1)**: `32인치모니터암`(큐 #34)은 07-27 **추천 다이얼로그로 사람이 추가**. `monitor-arm` 카테고리는 공개인데 secondary에 인치 변형이 없었고 `resolve_category`는 오매핑 방지로 **정확 매칭**만 한다 → `unmapped` 보류. 침묵 정지가 아니라 fail-loud 정상 작동.
2. **★★'validated'가 5게이트 통과를 뜻하지 않는다(GG3·근본수정)**: 미매핑 글은 `seo_cfg`가 비어 seo 게이트가 **skip(=pass 취급)**된 채 validated가 되는데, `auto_approve`는 게이트를 재검증하지 않는다 → **씨앗에 매핑을 추가하는 순간 미검증 글이 자동 발행**. 운영 실데이터 반증: 가드를 끄면 draft #48이 `ok=True`. 그 글은 산문 대표키워드 **0회**·소제목 0개·도입부 0(게이트 적용 시 3 issue 실패). → 저장 보고서로 재확인해 보류(`seo_unverified`), 보고서 없음·손상도 fail-closed.
3. **무인 리필 vs 사람 추가의 비대칭이 통로(GG2)**: 리필은 미매핑·비공개를 **자동 배제**(라이브 재현: 적격 18 / 미매핑 20 탈락)하지만 사람 경로는 제한하지 않는다 — 그 자체는 설계 의도지만 **넣는 시점에 신호가 없었다**. → `mapping_warning()` 단일 소스 + CLI 추가/추천 목록/`--add-top` + 대시보드 선택 창 **'발행' 칸**(고르기 전에 보임).
4. **사람 1클릭 승인도 경고(GG5)**: `cmd_approve`는 `eligible()`을 안 거쳐 마지막 미검증 경로가 열려 있었다(자체 재검수 적발). §2-마 지켜 **막지 않고 경고**.
5. **큐 보충 검증(GG7)**: pending 0이어도 리필이 자동 보충 — 다음 선정 예상 `의자추천`(9,880). 단 리필은 **yml secondary에서만** 나오므로 적격 18개 ≈ **18일치**. 씨앗 보강이 곧 발행 지속성.
6. **씨앗 인치 변형 6개(GG6)**: 네이버 실측(하한 300·20건)으로 32/27/24/34인치·고중량·폴타입 등재. 브랜드 5종·정보성·desk 양다리는 기존 정책대로 제외. ★부분 매칭 전환은 오매핑 위험이 더 커 채택 안 함.
7. **#48 본문 예산 관찰 종료(GG8)**: 08-01(3,695자)·08-03(3,269자) **둘 다 시도 1/3 통과** — 재현 안 됨·**예산 상향 불요**.

**자체 재검수로 정정한 것 4건** — 주인 지시 원칙([[critical-final-review]])의 실효 증거: ①"네이버 조회 실패"=**내 스크립트가 `load_secrets`를 안 부른 결함** ②"매핑만 하면 비용 0으로 오늘 글을 살린다"→재검증 결과 **명백한 미달** ③"방치하면 매일 ALERT"→`abnormal` 식 확인 결과 내일 정상 발행되면 경보가 꺼져 **조용히 방치**되는 게 실제 위험 ④**"이 갭으로 이미 2편이 미검증 발행됐다"는 과장**(GG4) — 실측하니 게이밍의자 10회·1.24%, 여름이불 7회·1.34%로 **둘 다 기준 충족**. 갭은 3회 발동했으나 실제 미달은 draft #48 하나. 부수로 테스트 함정 2개도 밟고 주석화(sentinel 충돌·패치 람다 RecursionError).

**무인·안전(§0)**: fail-loud 구멍 1개 봉인(`seo_unverified`) + 사람 경로 정보 비대칭 2곳 해소. 단위 테스트만으론 배선 공백을 못 보므로(#48 교훈) **운영 실데이터 read-only 검증 + 가드 OFF 반증**을 함께 수행. draft #48은 주인 결정으로 폐기(rejected).

**잔존/다음(#50)**: ①**★[관찰] 08-05 사이클 1일차** — 새 코드·씨앗이 실렸는지, `32인치모니터암` 재생성 시 "SEO 주입" 로그와 게이트 실측 여부 ②[관찰] 리필 풀 소진 속도(18일치) ③**[주인 확인] 08-02 미기동** — 스케줄러 조회가 TIMA 가드에 막혀 확인 불가 ④[입력] LLM 단가 ⑤(이월) 색인 전환·도마 편중·여름이불·draft 카테고리 3개·브랜드 blocklist·IndexNow 관찰성·쿠팡 부트스트랩.

---
