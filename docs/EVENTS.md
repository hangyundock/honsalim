# EVENTS.md — 혼살림 세션 로그

> 자동 회전: 6번째 세션 시 가장 옛 세션이 docs/archive/EVENTS_YYYYMM.md로 이동.
> 옛 세션 검색: 아래 월 인덱스로 파일 특정 → **그 파일 머리의 세션 인덱스** 확인 → 본문 grep.
> Cap: 20KB. ※#48에서 세션별 한 줄 요약을 각 월 파일로 이관 — 인덱스가 세션 수에 비례해
> 무한 증가하며 cap을 잠식하던 구조 문제 해결(23.7→16.0KB).

## ARCHIVE 인덱스 (월 단위 — 세션별 한 줄 요약은 각 파일 머리에 있음)

- [EVENTS_202607.md](archive/EVENTS_202607.md) — 세션 #41~#43 (3건)
- [EVENTS_202606.md](archive/EVENTS_202606.md) — 세션 #19~#40 (22건)
- [EVENTS_202605.md](archive/EVENTS_202605.md) — 세션 #1~#18 (16건)

## 최근 5세션

### 세션 #48 — 2026-07-31~08-01 (Fable 5, ★무인 발행 3일 중단 근본해결(밀도 벽 돌파·B2 절대 하한) + 라이브 발행 2편 + 비용 낭비 방지 5종 + slug 근본수정, 회귀 1126→1230, main 10커밋)

**시작 상황**: `/honsalim-start`(#47). 브리핑 중 **07-30·07-31 발행 0편**을 실측 적발 — 침묵 정지가 아니라 `책상추천`이 seo 게이트 `density_high`로 2연속 반려. 주인이 원인 규명 → 수정 → 라이브 테스트를 순차 지시. 후반 주인이 **"결과를 한 번에 내지 말고 비판적 정밀 재검수 후 최종결과"** 원칙을 **모든 결과물로 상시 확대** 지시.

**핵심 [확정]** (상세 **DECISIONS FF1~FF8**):
1. **밀도 벽의 정체(FF1·FF2)**: LLM이 횟수를 센 게 아니라 **문체를 바꿨다**(소제목·상품 문단·FAQ마다 기계 배치 = 31회). 그리고 모델은 **"탈락"이라 쓴 조건만 추적**한다 — 옛 지시는 하한에만, 내 1차 수정은 상한에만 경고를 달아 **양쪽으로 번갈아 탈락**(31회 도배 ↔ 7회·소제목 0개 미달). 세 경계를 같은 무게로 명시해 해결.
2. **★B2 절대 횟수 하한(FF3·주인 결정)**: 라이브 6개 본문 실측 — '~추천' 검색어 복합어는 도배 금지 시 자연 사용량이 **4~7회로 고정**돼 %하한은 긴 글에서 반드시 뚫린다(결정 A(0.8%)도 5,199자 글 0.41%로 즉시 뚫림). → 키워드 글 하한을 `min_count>=4`로 전환. 상한·도배 금지·위치 게이트 불변.
3. **1인칭 오탐 반전(FF4)**: "2~3년 사용에도 견고하다는 후기"·"1~2년 사용 후 이동"이 반려돼 재시도를 소모(3회 중 2회). 1차 블랙리스트가 곧 재발 → **경험 동사 화이트리스트**(했·해보·썼·중이). 재시도 2→3(주인 승인).
4. **★라이브 발행 2편**: `kw-269a4bdb`(책상추천·01:21) + `1-2`(1인용밥솥·08-01 11:11 **정규 사이클 시도 1회 통과·개입 0**). 라이브 200 검증. 글 **18→20편**.
5. **비용 낭비 방지 5종(FF6)**: 라이브 6회·15콜·3시간의 원인은 ①n=1 라이브 판단 루프 ②LLM 사용량 미기록(원화 환산 불가). → CLAUDE.md §7-3 대응 순서 + `cli experiment`(오프라인 N샘플 분포·기본 dry-run) + migration 010 토큰 추적(재시도 전량) + 본문 예산 정합화 + 캐시 관찰성.
6. **★캐시 조사 종결(FF7)**: 실측 input 27,687 중 **16,448(59%) 적중** — 제공자 자동 캐시 작동, 배선 복구 **불요**.
7. **slug 근본수정(FF8)**: `slugify`가 한글을 버리고 **남은 조각**을 slug로 씀(`1인용밥솥`→`1`→`1-2`). 부분 유실도 해시 폴백 + **큐에 굳은 조각 자가교정**(`32인치모니터암`이 `32`로 대기 = 다음 발행에 `/articles/32/` 될 뻔). 라이브 3건 URL은 주인 결정으로 유지.

**자체 재검수로 잡은 결함** — 주인 지시 원칙의 실효 증거: 목표 밀도 1.2→1.4 정정(실패 방향 비대칭) · 부동소수점 드리프트 · 카테고리 경로 누락 · 길이가 약분되는 무늬만 테스트 · **배선 검증 공백**(단위테스트만으론 백스톱을 안 불러도 초록불) · **실험 하네스가 운영과 다른 프롬프트 생성**(season_peak 누락 — 측정 도구가 틀리면 결론도 틀린다) · 하네스의 가짜 미달 표시 · slugify 수정만으론 큐의 굳은 slug 미해결 · 비용 보고 "47% 회피 가능"이 과장(실제 20%).

**무인·안전(§0)**: 자가복원 4겹째 = `enricher/density_fix.py` — density_high만 남으면 LLM 재호출 없이 초과분을 대체어로 감산(제목·고지·소제목·첫 등장 불가침, 조사 안전 판정, 5게이트 재검증 후 실패 시 원문 복귀). 실측 재생 4.33%→1.42%·3.97%→1.43% 둘 다 통과.

**잔존/다음(#49)**: ①**[관찰] 본문 예산 부작용** — 산문 25~30%↓로 출력 토큰은 줄었으나 키워드 자연 출현이 4~7→**3~5회**로 내려가 min_count=4 여유 축소(3샘플 중 1건 count_low). 며칠 발행 안정성 관찰 ②[입력] LLM 단가(`llm_price_*_per_1m`) — 넣기 전까지 비용 $0 표시 ③[이월] GSC 색인 전환·도마 클러스터 편중·여름이불 자동비공개·draft 카테고리 3개·IndexNow 관찰성·쿠팡 부트스트랩 ④(선택) 추적 미연결 경로(카테고리 가이드·비전 게이트).

---

### 세션 #47 — 2026-07-29 (Opus 5 / Fable 5, ★무인 발행 3일 공백 근본수정(반려 집계·밀도 지시) + 전체 정밀진단 안정화 5건, 회귀 1116→1126, main 2커밋 푸시·운영 반영)

**시작 상황**: `/honsalim-start`(#46). 브리핑 중 git log 실측으로 **07-21~23 발행 0편 3일 공백**을 적발(#46 이후 9일 무인 운영) → 주인 "3일 공백 원인부터" 지시. 이후 주인이 **"결과를 한 번에 내지 말고 비판적 모드로 최종점검 후 최종결정"** 원칙을 지시 → 전 작업에 적용(내 제안 2건 자체 기각·1건 문구 결함 자가 적발). 후반 주인 지시로 Fable 5 **전체 정밀진단**(무인 핵심 경로 전수).

**핵심 [확정]** (검증·main·운영 반영. **상세 DECISIONS EE1~EE7**):
1. **3일 공백 원인 = 침묵 정지 아님, 게이트 반려 3연속(EE1)**: 스케줄러 3일 모두 정상 기동(exit=0). `스텐도마`(4자)가 seo 게이트 6회 시도 전부 실패 → 상한 도달 격리 → 07-24 다른 키워드로 자동 복구. 밀도 0.28%→**3.94%**(3회→37회 오버슈트). 게이트는 정확했고 생성기가 문제.
2. **★반려를 '생성 성공'으로 집계하던 fail-loud 관통 결함(EE2·근본수정)**: `return 0` → `made+=1` → #45 가드 무력화 → **07-21·22 이틀 무경보**. `RC_GATE_REJECTED=4` 분리. 3일 공백이 이틀 조용했던 진짜 이유 — 초기 진단에서 놓쳤다가 코드 추적 중 적발.
3. **★밀도 지시 정량화(EE3·근본수정)**: "목표 1.7%로 높이세요"(정성) → "**총 약 16회** 포함"(절대). ★검증 중 **재생성이 직전 본문 없이 처음부터 다시 씀**을 확인 → 내가 처음 쓴 "13회를 더"가 성립 불가임을 자가 적발·정정. 4회·37회 양방향이 동일 목표로 수렴 실증.
4. **★비공개-only 날 라이브 미반영 = 가드레일 무력화(EE4·근본수정)**: 발행 0+자동비공개만 있는 날 `git_push` stub(commit 없음) 경로 → '미달' 글이 라이브 잔존. `refresh_cycle`로 교체+실패 rc 경보+죽은 헬퍼 제거. 옛 경로 지뢰 테스트로 재발 봉인.
5. **무인 래퍼 견고화 3 + busy_timeout(EE5·EE6)**: cli 크래시 무경보 해소 / **git pull이 매일 거짓 '실패' 기록**(EAP+stderr 승격)을 `$LASTEXITCODE` 판정으로 근본수정·**라이브 실증** / 로그 한글 영구 깨짐(cp949) UTF-8 복구 / `busy_timeout=10000`(대시보드 병행 시 즉시 lock 크래시 예방).
6. **비판 검증 기각 3건(EE7)**: "사이클 내 키워드 폴백"(비용 2배 vs 손실 3/40일=과잉설계·**내 제안 철회**)·"push 실패 커밋 고아화"·"미매핑 폴백 낭비". 회귀 1116→**1126**(+10)·1 skip. black·ruff·mypy 클린. main `91416db`·`289a7aa` 푸시·운영 ff 반영.
**부수**: `스텐도마` requeue(주인 결정·pending 복귀). **#46 카테고리 lastmod는 이미 라이브**임을 확인(07-24~ 매일 publish-queue 빌드가 실어 나름) — STATE의 '미반영' 항목이 낡았던 것·재빌드 불요.

**무인·안전(§0)**: 자가복원 3겹 유지 + **fail-loud 구멍 3개 봉인**(반려 집계·비공개 배포 실패·cli 크래시). 모든 수정에 재발 방지 가드 동반(신규 테스트 10건). 게이트 기준·비용 상한 불변.

**잔존/다음(#48)**: ①**[관찰] 내일 11:11 사이클 3종 검증** — `스텐도마` 재생성 통과 여부(실패해도 1일차 경보)·로그 한글 정상 기록(EE5③)·pull "성공(exit=0)" 기록(EE5②) ②**★[모니터링] GSC 색인 전환**(#46 요청 11개·9일 경과)([[growth-first-priority]]) ③[결정] 여름이불 자동비공개(방치/개선/침구 쿠팡) ④[결정] draft 카테고리 3개 공개 ⑤(이월) IndexNow 관찰성 갭·게이밍의자 이관·쿠팡 부트스트랩.

---

### 세션 #46 — 2026-07-20 (Opus 4.8, ★무인 다양화 발행 검증 + 색인 원인 진단(크롤 예산·기술 클린) + 카테고리 sitemap lastmod 근본수정 + 여름이불 무인 자동비공개 적발, 회귀 1114→1116, 코드 커밋·배포 대기)

**시작 상황**: `/honsalim-start`(#45). #46 최우선 관찰(무인 다양화·IndexNow) 라이브 점검 지시→성공 확인→주인 "성장 추천" 요청→색인 GSC 실데이터 심층 진단→주인 11개 색인요청→lastmod 근본수정.

**핵심 [확정]** (검증·라이브·상세 DECISIONS DD):
1. **무인 다양화 발행 검증**: 오늘 11:11 auto-cycle이 #45 새 씨앗(도마)에서 **나무도마(주방) 글** 자동발행(article 14 `kw-3ebd1e62`·5게이트 1차통과·카탈로그 재사용 11·abnormal:false·라이브 200·글 고유 title). 의자·컴퓨터 편중 탈출 실증. IndexNow 배선+키파일 라이브(200+키일치)·텔레그램 알림 경로 실행.
2. **★색인 원인=크롤 예산/권위(기술 아님) [GSC 실데이터·상세 DD1]**: 라이브 29 URL·색인 4. 미색인=robots 차단 16(/go/ 제휴·정상)·**발견됨-미색인 11(미크롤=크롤예산)**·크롤됨 2·404 1·리디렉트 1. 정량 반박(얇음X·상호중복 Jaccard 3.3%·내부링크 17~30·noindex/canonical 클린·깨진링크 0/3,843·URL검사 '최근 크롤링 해당없음') → 코드 은탄환 없음. 주인 **11개 색인요청 완료**. 404=우리 버그 아님.
3. **★카테고리 lastmod 근본수정(DD2)**: lastmod 없던 카테고리에 연결상품 `MAX(last_seen_at)`(=상품 새로고침일·날조 아님) 부여. 정적·페르소나는 변경일 없어 생략 유지(원설계). IndexNow diff 정합. 가드 2종·실데이터 6카테고리 상이날짜(06-26~07-20). 회귀 1114→**1116**. 파일 renderer·indexnow·test.
4. **★여름이불 무인 자동비공개 [관찰]**: #45 주인 발행 여름이불(id13·미매핑·쿠팡3)을 오늘 11:11 사후모니터가 '미달' 자동비공개(fail-closed·로그 "미달1·자동비공개1"). §0 정상이나 주인 결정 되돌림([추정] 미매핑·얇음). 발행 12편.

**무인·안전(§0)**: 오늘 사이클 정상(생성·발행·사후 자가복원). 진단·수정 전부 근본+가드+실데이터. IndexNow 관찰성 갭 발견([확정] 성공경로 res.notes 미로그·Bing/Yandex 이차)·이월.

**잔존/다음(#47)**: ①[배포] #46 코드 운영 동기화→빌드해야 라이브 lastmod 반영(wrapper git pull footgun 확인) ②[모니터링] 며칠 뒤 GSC 11개 색인 전환(주인) ③[결정] 여름이불(방치/개선/침구 쿠팡) ④(이월) IndexNow 관찰성·draft 카테고리 공개·게이밍의자 이관·쿠팡 부트스트랩.

---

### 세션 #45 — 2026-07-19 (Fable 5 / Opus 4.8, ★추천 키워드 편중 근본수정 + 색인 성장(E-E-A-T·IndexNow) + 적대검증 7건 + 여름이불 발행, 회귀 1051→1114, main 배포·라이브 curl 검증)

**시작 상황**: `/honsalim-start`(#44). 브리핑 중 무인이 **07-19 정상 재개**(ㄱ자책상 발행·게이트 미달→자가복원 작동) 확인 — 07-18은 여름이불 미매핑 보류로 발행0(경보 정상, 침묵 아님). 주인이 추천 다이얼로그 사진과 "추천 키워드가 의자·컴퓨터에만 집중 — 폭넓게+SEO 최적화" 지시. 비판 3인 검증→근본수정.

**핵심 [확정]** (검증·main·라이브 curl. **상세 DECISIONS CC1~CC6**):
1. **키워드 다양화(CC1·CC2)**: 씨앗이 책상 5개뿐이던 편중 근본해소 — 씨앗 5→7(도마·미니밥솥 네이버 실측)+require_terms(니치 필터: 밥솥 헤드·6인용 차단)+교차중복 정리+lint_alignment/doctor §15. 미니밥솥형(비전 전담) 키워드 글=검증 카탈로그 재사용(무필터 오염 봉인). 라이브: 추천 30건 7카테고리 분산.
2. **무인 fail-loud(CC3)**: refill 매핑·공개 한정(스캔폭 200 래칫 해소)+생성0 경보+auto_approve category_draft+publishability(conn) 단일 소스.
3. **E-E-A-T(CC4)**: 운영자 Person JSON-LD(혼살다·사진없음)+저자 통일(/about/)+about·method 정직성 정비(1인칭·거짓경험 제거)+/privacy/ 신설(PIPA §30).
4. **IndexNow(CC5·F4 완성)**: 키파일 빌드 생성(전 경로 대칭)+변경분만 통지+라이브 키 폴링+doctor §16. Google 무관(F6).
5. **적대검증 3워크플로우→7건**: build 비대칭·IndexNow 견고화·refill 스캔폭·publishability(conn)·**★테스트 텔레그램 실발송 격리**(pytest마다 주인 폰에 가짜 리포트 보내던 #42 잠복결함)·privacy PIPA·무늬만 테스트 실단언화.
6. **여름이불 발행(CC6·주인 결정)**: 07-18 보류분을 수동승인 발행(글 #13·라이브 검증). 침구 카테고리 미구축(과잉설계·침구=쿠팡 전용). 회귀 1051→**1114**(+63). 클린. main 0e800ea·750ec30·416dd2c+배포 0204b7f+발행 dea49af.

**무인·안전(§0)**: 자가복원 3겹 + fail-loud 보강. 적대검증으로 잠복결함(실발송·build 비대칭·헛발송) 사전 차단. 라이브 curl 검증. ★무인 발행이 origin 전진→push 전 ff 재동기화([[autonomous-deploy-advances-origin]]).

**잔존/다음(#46)**: ①[관찰] 무인 다양화 글 실발행·IndexNow 통지·카탈로그 재사용 라이브 ②★색인 커버리지(주인 콘솔 색인 요청·#45 효과) ③[결정] draft 카테고리 3개 공개·씨앗(미니제습기=2027-04) ④(선택)게이밍의자 이관·(이월)쿠팡 부트스트랩.

---

### 세션 #44 — 2026-07-17 (Opus 4.8 / Fable 5, ★무인 발행 10일 침묵 정지 복구 + 안전정지 텔레그램 경보(fail-loud) + 발행 글 title 근본수정 + 게이밍의자 재발행, 회귀 1043→1051, main·라이브 반영)

**시작 상황**: `/honsalim-start`(#43). 브리핑 중 **무인 발행 07-08~17 10일 침묵 정지** 적발 — 원인=#43 가이드 작업이 07-07 운영 폴더를 **detached HEAD로 이탈**시켜 래퍼의 `main 아님` 가드가 매일 조용히 안전 정지.

**핵심 [확정]** (상세 DECISIONS BB):
1. **무인 복구**: 운영 폴더 `main` 복귀 → 무인 재개. 수동 1편 발행=서재책상(5게이트 PASS·텔레그램 실발송).
2. **★BB1 안전정지 텔레그램 경보(fail-loud)**: `cli notify-alert` + 래퍼 3개 정지지점(git 불가·main 아님·DB 없음)에서 `Send-Alert`. 부수: Qt 위젯 테스트 오프스크린 크래시 → opt-in gate.
3. **★BB2 발행 글 `<title>` = 글 제목**: 카테고리 매핑 글이 guide_title을 상속해 같은 카테고리 글 전부 동일 title(중복·라이브 적발) → is_article이면 글 제목 사용.
4. **★BB3 게이밍의자 재발행(주인 결정)**: #31의 301이 현행 모델에서 승인 콘텐츠를 좀비화 → 301 폐기+republish. 노트북받침대는 301 유지.
회귀 1043→**1051**(+8)·1 skip. 클린. main 5커밋(배포 8629c62).

**무인·안전(§0)**: 자가복원 3겹(#41 반려+#42 생성 예외+#44 안전정지 경보). 메모리 [[autonomous-detached-head-silent-stop]] 신설.

**잔존/다음(#45)**: ①#44 경보 실전 관찰 ②★성장 색인 커버리지 ③게이밍의자 구조 이관 ④(이월) E-E-A-T·쿠팡 부트스트랩.

---

> (세션 #19~#43은 docs/archive/(EVENTS_202606·202607).md로 회전됨 — ARCHIVE 인덱스 참조)
